1). CASUAL 

ask user for 2 integers from the user
Get 2 integers and assign them to different variables
add those two variables together
return the result

  FORMAL

START

  PRINT 'First number!'
  SET number1 = GET user input
  SET number2 = GET user input

  convert number1 and number2 to integers

  PRINT number1 + number2

END

2).  CASUAL

Given an array of strings

Iterate through the array
  -set a variable whose value is an empty string
  -concat current string with variable
  - set variable to the value of the concat
  -move to next iteration

After iterating through the array return the result

    FORMAL

START

  #Given an array of strings

EACH value in the array
  SET concat =''
  concat = concat + current value

PRINT concat

3). CASUAL
